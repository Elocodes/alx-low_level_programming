#include <stdio.h>
#include <string.h>

/**
 *_strlen_recursion - a function thaat returns len of string
 *@s: string to be checked
 *
 * Return: length of string
 */
int _strlen_recursion(char *s)
{
	int string_length;

	string_length = strlen(s);
	return (string_length);
}
