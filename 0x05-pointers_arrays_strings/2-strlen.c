#include <string.h>

/**
 * _strlen - function calcutes the length of string when called
 * @s: character whose length is to be returned
 *
 * Return: returns the integer number of characters in the string
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
